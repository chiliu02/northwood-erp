# Copy to terraform.tfvars and adjust. All values shown are the defaults —
# uncomment only what you want to change.

# region      = "ap-southeast-2"
# name_prefix = "northwood-demo"

# Image tag the build script pushed to ECR for the 8 app images (a git SHA for real runs).
# image_tag = "latest"

# Empty schema instead of the populated demo dataset.
# load_seed_data = false

# Keycloak public address (DNS or the web EC2's public IP) used as the OIDC issuer.
# Browser OIDC login won't work until this is set — set it after the first apply
# once the public IP is known, or point it at an Elastic IP.
# keycloak_hostname = "http://<web-ec2-public-ip>:8080"

# Observability (§1D): run the Tempo/Loki/Prometheus/Grafana box on the data EC2
# and point the apps at it. Set false to skip it (and its cost).
# enable_observability = true

# Keycloak northwood-bff client secret — must match db/keycloak/northwood-realm.json.
# bff_client_secret = "northwood-bff-secret"
