# Browser test script: Northwood ERP scenario 3.1 (sales fulfilment happy path)

You are driving a browser end-to-end test of the operational ERP SPA. The goal is to place a sales order, watch all three sagas drive it through to settlement, and confirm the four new persona-driven forms work as designed.

## Context

Northwood is a multi-service ERP demo. Seven backend services (sales, inventory, manufacturing, purchasing, finance, product, reporting) communicate via Kafka + an outbox/inbox pattern. The operational SPA (`erp-web-ui`) is what real ERP operators would use — module-grouped sidebar (Sales / Purchasing / Inventory / Manufacturing / Finance), per-persona role gating on write actions. Authentication is OIDC against Keycloak.

The user has just shipped four new operational forms (`/shipments/new`, `/goods-receipts/new`, `/supplier-invoices/new`, `/payments/new`) bringing the SPA to user-stories.md parity. This test verifies scenario 3.1 — a small sales order placed, manufactured, shipped, invoiced, and paid — runs end-to-end through the new forms.

## Prerequisites

- SPA at `http://localhost:5174` (Vite dev server)
- All 7 services + `erp-web-ui-bff` (port 8089) running
- Postgres + Kafka + Keycloak running (docker compose)
- Fresh start ideal but not required — the test creates its own order

## Demo users

All passwords equal the username. Use the persona switcher in the app bar (top-right dropdown, "Switch persona" button with a users icon) — this triggers a Keycloak logout + re-login flow with `login_hint` pre-filled.

| Username | Role | Steps using this persona |
|---|---|---|
| `sarah` | sales_clerk | Place the order |
| `linda` | production_planner | Complete WO operations |
| `mike` | warehouse_clerk | Post the shipment |
| `olivia` | accountant | Record the customer payment |

When the persona switcher takes you to Keycloak's login page, the username will be pre-filled — just type the password (same as the username) and click Sign In.

## The flow

### Step 1: Login as Sarah and place an order

1. Navigate to `http://localhost:5174`. If not logged in, Keycloak login page appears.
2. Enter username `sarah`, password `sarah`, click Sign In.
3. You land on the Home dashboard. Click "Sales" in the left sidebar to expand the module, then "Sales Orders".
4. Click the **"New Order"** button (top-right of the page, primary blue button with a + icon).
5. The form `/sales-orders/new` opens. It should auto-populate:
   - "Order number" = `SO-<timestamp>` (auto-generated)
   - "Customer" dropdown = `CUST-001 · Sydney Home Living` (the only active demo customer)
   - "Currency" = `AUD`
   - Lines table = empty
6. Click **"Add line"**. A row appears with product `FG-TABLE-001 · Wooden Dining Table`, qty `1`, unit price `320` (auto-filled from the catalog).
7. Click **"Place order"** (top-right primary button).
8. **Expected**: toast "Sales order SO-... placed." appears bottom-right. Browser navigates to `/sales-orders/<uuid>` (the SO detail page).
9. **Capture**: the order's UUID from the URL. You'll come back to this page later to watch saga progress.

### Step 2: Watch the saga advance (passive, ~10–20 seconds)

The fulfilment saga drives automatically from `submitted → stock_reservation_requested → stock_reserved → manufacturing_requested → manufacturing_in_progress`. The make-to-order saga drives the work order from `started → work_order_created → raw_materials_reserved`.

1. On the SO detail page, look for the `status` field (or `manufacturing_status`). Refresh the page periodically (or wait ~10s and look — the page should auto-refresh).
2. **Expected**: SO `status` transitions to `in_fulfilment` (sales saga side) and `manufacturing_status` to `released` or `completed`.

If after 30 seconds the saga is still on `submitted`, **flag this as a stuck saga** — the backend isn't reacting and that's a problem. Don't continue.

### Step 3: Switch to Linda, complete WO operations

1. Click the "Switch persona" dropdown in the app bar. Pick **Linda**.
2. Keycloak login page appears. Username pre-filled as `linda`. Type password `linda`, Sign In.
3. You land back on the dashboard. Navigate Manufacturing → **Work Orders**.
4. Find the WO that was auto-created for the SO. It should be the only one with `status=released`. Click into it.
5. The WO detail page shows 4 operations (CUT, DRILL, ASSEMBLE, FINISH).
6. For each operation in order (10, 20, 30, 40), click **"Complete operation"**. A small dialog or inline form may ask for actual minutes — enter `30` and confirm.
7. **Expected after the 4th operation**: WO status flips to `completed`. Toast confirmation each time.

If "Complete operation" is disabled with a tooltip "Requires role: production_planner" — that means you're not Linda. Re-do the persona switch.

### Step 4: Switch to Mike, post the shipment

1. Switch persona to **Mike** (password `mike`).
2. Navigate Inventory → **Shipments**.
3. Click **"Post shipment"** (top-right primary button).
4. The form `/shipments/new` opens. Auto-fills should include:
   - Shipment number = `SH-<timestamp>`
   - Sales order dropdown = the SO you placed in step 1 (it's the only one at `ready_to_ship`)
   - Warehouse code = `MAIN`
   - Lines table = one row, auto-populated from the SO (product, line UUID, quantity 1, unit cost 150)
5. Click **"Post shipment"**.
6. **Expected**: toast "Shipment SH-... posted." Browser navigates to `/shipments` list, which now shows the new row.

If the "Sales order" dropdown is empty saying "no orders are ready_to_ship yet" — the saga hasn't caught up. Wait 10s and reload.

### Step 5: Switch to Olivia, record the customer payment

After step 4, the finance service auto-creates a customer invoice from the `SalesOrderShipped` event. You don't need to do anything for that — it just happens.

1. Switch persona to **Olivia** (password `olivia`).
2. Navigate Finance → **Customer Invoices**. You should see one invoice for the SO, status `posted`, total `320.00 AUD`. (Optional sanity check — you don't need to click in.)
3. Navigate Finance → **Payments**.
4. Click **"Record payment"** (top-right primary button).
5. The form `/payments/new` opens. It has two tabs at the top: "Supplier (AP)" and "Customer (AR)". Click **"Customer (AR)"**.
6. Auto-fills should include:
   - Payment number = `PMT-<timestamp>`
   - Customer invoice dropdown = the just-created invoice (newest "Open" invoice)
   - Amount = `320.00` (auto-filled from invoice total)
   - Method = "Bank transfer"
7. Click **"Record customer payment"**.
8. **Expected**: toast "Customer payment PMT-... recorded." Browser navigates to `/payments` list.

### Step 6: Verify final state

1. Navigate back to Sales Orders, click into your order.
2. **Expected**: `status` is `completed`, `payment_status` is `paid`, `outstanding_amount` is `0.00`.

That confirms the sales fulfilment saga reached its terminal state and the order is settled.

## Reporting back

Report each step's outcome in your final summary:

- ✅ if the step matched the expected outcome
- ⚠️ if it worked but something was off (wrong toast text, picker didn't auto-select, button took unusually long to enable, layout broken)
- ❌ if the step failed (form errored, request 4xx/5xx, saga didn't advance)

Specifically call out:
1. Did **every form auto-fill its defaults** correctly (picker, line table, amount)? This is what the §1C slice was designed to ship.
2. Did **role gating work** — when you tried "Post shipment" as Sarah (i.e. without switching persona), was the button disabled with the "Requires role: warehouse_clerk" tooltip? (Optional negative test — try this once if curious.)
3. Did the **persona switcher actually log you out and re-login** as the new user (not just cosmetic)? After switching, the username chip in the app bar should change.
4. Any **404s, JS console errors, or layout breakages** along the way.

If a step is genuinely blocked (saga doesn't advance, form crashes), stop and report. Don't try to fix code.
