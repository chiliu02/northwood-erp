package com.northwood.product;

import org.junit.jupiter.api.Test;

/**
 * Smoke test placeholder.
 *
 * <p>Replace with a {@code @SpringBootTest} that boots the application against
 * a Testcontainers PostgreSQL once you're ready for integration tests.
 * Example dependencies are already wired in the POM.
 */
class ProductApplicationTests {
    @Test
    void main_methodIsCallable() {
        // intentionally empty
    }
}
